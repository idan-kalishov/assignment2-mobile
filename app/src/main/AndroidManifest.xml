<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Assignment2mobile"
        tools:targetApi="35">

        <!-- Other Activities -->
        <activity android:name=".activities.AddStudentActivity" />
        <activity android:name=".activities.StudentDetailsActivity" />


        <!-- Set StudentListActivity as the launcher activity -->
        <activity
            android:name=".activities.StudentListActivity"
            android:theme="@style/Theme.Assignment2mobile"
            android:exported="true"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
